#!/bin/bash

wps=$(redyt wallpapers --history --filter)
[ ! -z "$wps" ] && mv $wps "$HOME/pictures/wallpapers"
cache="$XDG_CACHE_HOME"/redyt/wallpapers

for file in "$cache"/*; do
	! [ "$file" = "$cache/hist" ] && rm $file
done
